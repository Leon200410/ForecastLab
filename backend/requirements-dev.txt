# Dev/test dependencies. Install on top of the runtime requirements:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest
pip-tools        # `pip-compile` to regenerate requirements.lock for reproducible installs
