# Core (required) — installs clean on Python 3.11–3.14, no heavy build deps
fastapi
uvicorn[standard]
httpx
pydantic>=2
python-dotenv
tenacity                    # retry + exponential backoff on transient LLM/network errors

# Per-category agents (DeepSeek via OpenAI-compatible) + live-data tools.
# Pinned: the LangChain 1.x line moves fast and create_agent's API/behaviour can
# shift between minors — pin so a fresh install can't break the agent overnight.
# (For full reproducibility generate a lockfile: pip freeze > requirements.lock.)
langchain==1.3.11
langchain-openai==1.3.3
langgraph==1.2.6            # create_agent lives in langchain.agents but runs on langgraph

# RAG embeddings — default EMBEDDING_PROVIDER=fastembed (ONNX, no torch; multilingual)
fastembed==0.8.0
numpy==2.5.0               # vectorized KB similarity search (app/kb/store.py)

# Optional — quality upgrades (not required; DeepSeek uses httpx above)
#   anthropic               # only if LLM_PROVIDER=anthropic
#   sentence-transformers   # better embeddings (EMBEDDING_PROVIDER=sentence-transformers)
#   chromadb                # swappable vector store (default: built-in local store)
#   trafilatura             # better article extraction (default: simple HTML stripper)
